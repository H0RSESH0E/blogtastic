<h1>FUN!</h1>
<article class="post">
    <div class="title">
        <h3>{{post.title}}</h3>
        <p>{{post.content}}</p>
        <p>Written by {{post.user.username}} on: {{format_date post.created_at}}</p>
    </div>
</article>